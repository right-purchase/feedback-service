{
  "name": "feedback-service",
  "version": "1.0.0",
  "description": "The Feedback Service provides the means to the user contact Right Purchase",
  "main": "dist/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "serve": "nodemon --watch src/ --ext ts --exec ts-node src/index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.16",
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
